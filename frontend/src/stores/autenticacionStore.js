import { defineStore } from "pinia";
import { ref, computed } from "vue";

export const useAutenticacionStore = defineStore('autenticación', () => {

    //State -> datos reactivos (variables) que se esperan esten disponibles globalmente
    const token = ref()

    //getters -> Cálculos en base a los datos


    //actions -> Acciones o funciones que cambian los datos
    


})